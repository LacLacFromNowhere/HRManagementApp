<UserControl x:Class="HRManagementApp.Views.EmployeesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
	<Grid Margin="20">
		<TextBlock Text="Employee Management"
		           FontSize="24"
		           FontWeight="Bold"
		           Margin="0,0,0,20"/>
		<ListView Name="EmployeeList" Margin="0,40,0,0">
			<ListView.View>
				<GridView>
					<GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="100"/>
					<GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="200"/>
					<GridViewColumn Header="Position" DisplayMemberBinding="{Binding Position}" Width="150"/>
				</GridView>
			</ListView.View>
		</ListView>
	</Grid>
</UserControl>